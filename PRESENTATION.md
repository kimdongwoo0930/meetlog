# MeetLog 발표 자료 (PPT 슬라이드 구성안)

> AI 회의록 자동 생성 파이프라인 — STT부터 LoRA 파인튜닝까지
> 상세 수치 근거: [PERFORMANCE.md](PERFORMANCE.md)

---

## 슬라이드 1 — 표지

**MeetLog — 실시간 음성 회의 자동 회의록 생성 플랫폼**

- 음성 회의를 녹음하면 AI가 회의록·결정사항·할 일을 자동 생성
- 기술 스택: Next.js · Spring Boot · FastAPI · Whisper · Qwen2.5
- 핵심: **온프레미스 로컬 LLM**으로 비용 0원 + 성능 최적화

---

## 슬라이드 2 — 문제 정의 & 전체 파이프라인

**"회의는 많은데 회의록 정리는 늘 귀찮다"**

```
음성 업로드 → STT(Whisper) → 청크 분할 → MAP(구간별 추출)
→ FOLD(통합) → REDUCE(구조화) → 회의록 JSON
                                  ├ 요약
                                  ├ 결정사항
                                  ├ 할 일 (담당자별)
                                  ├ 미결 질문
                                  └ 다음 안건
```

- 외부 API(OpenAI 등) 안 쓰고 **로컬에서 전부 처리** → 회의 내용 외부 유출 없음, 비용 0
- 발표 포인트: **단순 동작이 아니라 "성능/비용 최적화"를 했다**

---

## 슬라이드 3 — [STT] 백엔드 전환으로 6배 가속

**문제:** faster-whisper(CTranslate2)는 Apple GPU(Metal)를 못 써서 맥에서 CPU로만 동작 → 느림

**해결:** `mlx-whisper`(Metal GPU) + `large-v3-turbo`로 전환, 설정으로 백엔드 스위칭

| 구성 | 백엔드 | 모델 | 처리시간 | RTF | 품질 |
|---|---|---|---|---|---|
| 기존 | faster-whisper (CPU) | medium | 12.80s | 0.47x | 띄어쓰기 어색, 끝 구간 누락 |
| **신규** | **mlx-whisper (Metal)** | **large-v3-turbo** | **2.11s** | **0.08x** | 정확, 끝까지 전사 |

> **속도 약 6배↑ + 품질 동시 개선** (27.3초 한국어 샘플, M3 Pro 기준)
> RTF 0.08 ≪ 1 → 실시간 자막에 충분

**부가 개선:** 한국어 환각 문구 필터("구독과 좋아요"), highpass 노이즈 제거, 신뢰도 기반 오인식 구간 제외

---

## 슬라이드 4 — [LLM] 긴 회의 처리: map-reduce + 계층적 fold

**문제:** Ollama 기본 컨텍스트(2048 토큰)는 긴 회의 전사문을 **에러 없이 조용히 잘라버림** → 뒷부분 누락

**해결 1:** `num_ctx` 8192로 상향 + JSON 스키마 강제(structured output)

**해결 2:** 회의 길이와 무관하게 동작하는 2단계(+재귀) 구조

```
긴 전사문 → 청크 분할(3,500자) → [MAP] 구간별 핵심 추출
  → [FOLD] 정리 합이 한도 초과 시 그룹 통합 반복 → [REDUCE] 최종 회의록
```

- **MAP은 "요약"이 아니라 "추출"** — 단순 요약 반복하면 결정사항·담당자가 중간에 증발
- **FOLD 루프**로 회의가 아무리 길어도 reduce 입력이 컨텍스트 한도를 안 넘음
- 짧은 회의는 오버헤드 없이 단일 호출

---

## 슬라이드 5 — [최적화] map/reduce 모델 분리

**아이디어:** MAP은 청크 수만큼(10~40회) 호출, REDUCE는 1회. 어려운 추론은 REDUCE에 몰려 있음
→ **MAP만 경량 모델(7B), REDUCE만 14B**

**A/B 벤치마크** (교육정책 토론 28,846자 · 9청크)

| 구성 | MAP | REDUCE | 총 소요 | 추출 항목 |
|---|---|---|---|---|
| A (baseline) | 14B | 14B | **389.6s** | decisions 2 / todos 9 |
| B (최적화) | **7B** | 14B | **181.9s** | decisions 0 / todos 3 |

> **레이턴시 −53% (약 2.1배 단축)** ... 하지만 **항목 recall 희생** (7B map이 세부 정보 누락)

**→ "공짜 점심은 없다." 이 격차가 다음 단계(LoRA)의 동기**

---

## 슬라이드 6 — [LoRA] 7B를 14B 수준으로: Distillation

**전략:** Claude가 생성한 고품질 회의록을 **정답(타깃)** 으로 7B를 파인튜닝
→ 7B의 속도를 유지하면서 품질(recall) 회복

| 항목 | 값 |
|---|---|
| 베이스 모델 | Qwen2.5-7B-Instruct |
| 기법 | QLoRA (4-bit) + LoRA rank 16 |
| 학습 데이터 | 한국어 회의 51개 (Claude 생성 라벨) |
| 플랫폼 | Google Colab T4 (15GB) |
| 학습 파라미터 | 4,037만 개 = 전체의 **0.53%만 학습** |

> LoRA 핵심: 70억 파라미터 전체가 아니라 **0.53%만 학습**해서 적은 자원으로 모델 특화

---

## 슬라이드 7 — [LoRA] 전/후 비교 결과

**MAP 단계 — 정리 스타일 변화**

| | Before LoRA (Base 7B) | After LoRA |
|---|---|---|
| 구간 1 | 할 일 3개 나열 | 리뷰·설계·할 일 **섹션 분리** |
| 구간 2 | 카테고리 그룹 | 담당자별 개별 나열 |

**최종 회의록(REDUCE)**

| 항목 | Before | After |
|---|---|---|
| decisions | 6 | 2 |
| todos | 8 | **18** |

> **성과:** 7B가 한국어 회의 맥락을 더 잘 인식, 구조화된 정리 출력
> **한계(솔직하게):** 학습 데이터 51개는 부족 → 300개+ 필요, decisions/todos 구분 흐려짐
> **서빙:** GGUF 변환 후 Ollama 서빙하면 Base 7B와 **속도 동일** (LoRA는 연산량 거의 안 늘어남)

---

## 슬라이드 8 — 데모 (라이브 화면)

**웹에서 직접 보여줄 화면 (6개 데모 페이지)**

| 회의 | 데모 |
|---|---|
| 긴 회의 (개발, 10,504자) | 회의록 / STT 검토 / LoRA 전후 비교 |
| 짧은 회의 (마케팅, 4,702자) | 회의록 / STT 검토 / **AI 처리 중간 단계** |

**시연 흐름:**
1. STT 원문 검토 화면 — 추임새 많은 실제 전사문, 수정/삭제
2. "회의록 생성" 클릭 → 로딩 → 최종 회의록 (담당자별 할 일 그룹화)
3. 중간 단계 화면 — 청크 분할 → MAP 구간별 정리 → REDUCE 흐름 시각화

---

## 슬라이드 9 — 정리 & 기여

**한 일**

1. **STT 6배 가속** — 하드웨어 제약(Metal 미지원) 진단 → mlx 전환, 속도·품질 동시 개선
2. **긴 회의 안정 처리** — map-reduce + 계층적 fold로 회의 길이 무관 동작
3. **비용 최적화** — map 7B / reduce 14B 분리로 레이턴시 2배 단축
4. **품질 회복** — LoRA distillation으로 7B 특화 (0.53% 파라미터 학습)

**핵심 메시지**

> 단순히 "동작하는 것"을 넘어, **각 단계에서 측정 → 병목 진단 → 최적화 → 트레이드오프 분석**의
> 엔지니어링 사이클을 반복했다.

---

## 발표 시 강조 포인트 (스피커 노트)

- **수치는 전부 실측** — STT 6배, 레이턴시 −53% 모두 직접 벤치마크한 값
- **트레이드오프를 숨기지 않음** — 7B map의 recall 손실을 인정하고 LoRA로 해결 시도한 흐름이 스토리
- **로컬 LLM 강조** — 외부 API 없이 비용 0, 회의 내용 외부 유출 없음 (보안)
- **시간 차이 질문 대비** — LoRA 측정이 느린 건 transformers 직접 추론이라서, GGUF/Ollama 서빙 시 동일

---

## 부록 — 기술 스택 요약

| 영역 | 기술 |
|---|---|
| Frontend | Next.js 14, TypeScript |
| Backend | Spring Boot 3, JPA, MySQL |
| AI Server | FastAPI, mlx-whisper, Ollama (Qwen2.5 7B/14B) |
| 파인튜닝 | PEFT, Unsloth, QLoRA, Google Colab T4 |
| 인프라 | Mac Mini M2 (전용 서버), Docker |
